---
name: CesiumWidget
route: /components/CesiumWidget
menu: Components
---

import { Playground } from "docz";
import CesiumWidget from "../components/CesiumWidget";

# CesiumWidget

`CesiumWidget` is the simplest root component of resium. All components of resium except the root components have to be mounted inside it.

`Viewer` is also a root component. It is a component with some widgets (info box, timeline, and so on) added to `CesiumWidget`.

**Cesium element**: [CesiumWidget](https://cesiumjs.org/Cesium/Build/Documentation/CesiumWidget.html)

<Playground>
  <CesiumWidget full />
</Playground>

## Available scope

Everywhere. `CesiumWidget` is a root component. 

## Properties

### Cesium properties

| Property | Type | Description |
|--|--|--|
| resolutionScale | number | |
| useDefaultRenderLoop | boolean | |
| targetFrameRate | number | |

### Cesium read only properties

| Property | Type | Description |
|--|--|--|
| clock | Cesium.Clock | |
| imageryProvider | Cesium.ImageryProvider | |
| terrainProvider | Cesium.TerrainProvider | |
| skyBox | Cesium.SkyBox | |
| skyAtmosphere | Cesium.SkyAtmosphere | |
| sceneMode | Cesium.SceneMode | |
| scene3DOnly | boolean | |
| orderIndependentTranslucency | boolean | |
| mapProjection | Cesium.MapProjection | |
| globe | Cesium.Globe | |
| showRenderLoopErrors | boolean | |
| contextOptions | WebGLContextAttributes | |
| creditContainer | Element &#124; string | |
| creditViewport | Element &#124; string | |
| terrainExaggeration | number | |
| shadows | boolean | |
| terrainShadows | Cesium.ShadowMode | |
| requestRenderMode | boolean | |
| maximumRenderTimeChange | number | |

### Cesium events

N/A

### Other properties

| Property | Type | Description |
|--|--|--|
| className | string | `className` prop applied to outer `div` element |
| id | string | `id` prop applied to outer `div` element |
| style | React.CSSProperties | `style` prop applied to outer `div` element |
| full | boolean | Same as `style={{ position: "absolute", top: 0, left: 0, right: 0, bottom: 0 }}` if it is true. |
| containerProps | any | All properties applied to outer `div` element except `id`, `className`, and `style` prop. |
| children | React.ReactNode | |
